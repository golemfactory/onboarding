export const ReccpmmendationCard = ({
  usegeTime,
  description,
  icon,
  title,
}: {
  usegeTime: number
  description: string
  icon: string
  title: string
}) => {
  return <div className="flex flex-col gap-2"></div>
}
